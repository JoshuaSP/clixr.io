ClixrIo.Views.Div = ClixrIo.Views.Element.extend({
  tagName: 'div',
  editMenu: ClixrIo.Views.AddElementMenu
  // pass in this view's element as $targetEl
  // TODO the above is wrong just put there to see if we can get past errors
});
